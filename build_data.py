"""
build_data.py — 一键数据处理与后端启动脚本

暂时用不到 整个项目完工了再写这个

本脚本用于自动化运行整个数据处理与服务启动流程，无需手动逐个执行各个脚本。
执行时会按以下顺序依次完成：

crawler.py         —— 爬取评论数据, 生成原始数据文件
data_cleaning.py   —— 清洗原始数据, 输出规范化数据文件
pipeline.py        —— 对清洗后的数据进行分析与处理 (如情感标注), 生成可供后端使用的数据文件
app.py             —— 启动 Flask 后端服务，为前端提供数据 API

"""